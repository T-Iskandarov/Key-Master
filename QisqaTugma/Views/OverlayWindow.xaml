<Window x:Class="QisqaTugma.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="500" Height="120"
        Loaded="Window_Loaded">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border x:Name="KeyBorder"
                Background="#CC000000"
                CornerRadius="12"
                Padding="20,10"
                Visibility="Collapsed"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <ScaleTransform x:Name="BorderScale" ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <TextBlock x:Name="KeyText"
                       Text=""
                       FontSize="28"
                       FontWeight="SemiBold"
                       FontFamily="Segoe UI"
                       Foreground="White"
                       TextAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <Border x:Name="DescBorder"
                Background="#CC222222"
                CornerRadius="12"
                Padding="15,10"
                Margin="10,0,0,0"
                Visibility="Collapsed"
                MaxWidth="250"
                RenderTransformOrigin="0,0.5">
            <Border.RenderTransform>
                <ScaleTransform x:Name="DescScale" ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <TextBlock x:Name="DescText"
                       Text=""
                       FontSize="16"
                       FontFamily="Segoe UI"
                       Foreground="#DDDDDD"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>
        </Border>
    </StackPanel>
</Window>
